<%= form_for(@community_service) do |f| %>
  <% if @community_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@community_service.errors.count, "error") %> prohibited this community_service from being saved:</h2>

      <ul>
      <% @community_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.text_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :total_hours %><br>
    <%= f.text_field :total_hours %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
